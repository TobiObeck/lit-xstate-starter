// @ts-ignore
import * as allXStateStore from 'https://esm.run/@xstate/store'

/**
 * @type {import('@xstate/store')}
 */
const xstore = allXStateStore

export const { createStore, fromStore, createStoreWithProducer } = xstore
